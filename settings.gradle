pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ComposeSample"

include ':app'

include ':domain'

include ':data:remote'
include ':data:local'
include ':data:repository'

include ':feature:main_list'
include ':feature:main_detail'

include ':library:design'
include ':library:navigation'
